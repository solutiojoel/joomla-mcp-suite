# Port for the HTTP MCP server (used by the orchestrator)
HTTP_PORT=9305

# Anthropic API key — required for Phase 1+ sub-agent tool handlers.
# agents-mcp makes the Anthropic API calls directly (not Claude Code).
ANTHROPIC_API_KEY=sk-ant-...
