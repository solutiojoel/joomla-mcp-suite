{
  "name": "content-interpreter",
  "hidden": true,
  "description": "Phase 3.5 of the menu build: fills the content fields of a locked Content Schematic scaffold from the client menu/content PDF. Runs on the Claude Agent SDK using the operator's Claude Code subscription credentials (CLAUDE_CODE_OAUTH_TOKEN or local claude login). Never adds, removes, or rekeys entries — structure is owned by the deterministic derivation from the approved Menu Spec.",
  "instructions": "content-interpreter-system.md",
  "model": "claude-sonnet-5",
  "tools": {
    "allow": ["joomla_workspace_write", "joomla_workspace_read"],
    "downstreams": ["joomla-mcp"]
  },
  "docs": { "allow": [] }
}
