> **Moved to Knowledge Gateway.**
> The Universal Editing Conventions live in the Knowledge Gateway database (id: 11, tag: `editing-rules`).
> Load them with:
> ```
> knowledge_universal { action: "get", id: 11 }
> ```
> or
> ```
> knowledge_universal { action: "list", tag: "editing-rules" }
> ```
> Do not read further — this file is a stub only.
